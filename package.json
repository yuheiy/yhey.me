{
  "name": "yhey.me",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "serve": "browser-sync start --files 'dist' --server dist",
    "clean": "del 'dist' && git clone -b gh-pages https://github.com/yuheiy/yhey.me.git dist && del 'dist/**' '!dist' '!dist/.git'",
    "copy": "ncp static dist",
    "build:html": "jade src/jade/[!_]*.jade --out dist",
    "build:css": "node-sass src/scss -o dist/css --output-style compressed",
    "build:js": "mkdirp dist/js && browserify src/js/main.js | uglifyjs --compress --mangle > dist/js/main.js",
    "build": "npm-run-all clean --parallel copy build:*",
    "watch:html": "npm run build:html -- --watch",
    "watch:css": "node-sass src/scss -o dist/css --watch",
    "watch:js": "mkdirp dist/js && watchify src/js/main.js -o dist/js/main.js -v",
    "watch": "npm-run-all clean --parallel serve copy watch:*",
    "deploy": "cd dist/ && git push origin gh-pages",
    "predeploy": "npm run build && cd dist/ && git add --all && git commit -m 'Build'"
  },
  "browserify": {
    "transform": [
      "babelify",
      "jadeify"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuheiy/yhey.me.git"
  },
  "author": "Yuhei Yasuda <yuhei.yasuda1003@gmail.com> (https://yhey.me/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/yuheiy/yhey.me/issues"
  },
  "homepage": "https://yhey.me/",
  "dependencies": {
    "es6-promise": "^3.1.2",
    "moment": "^2.12.0",
    "sanitize.css": "^3.3.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browser-sync": "^2.11.2",
    "browserify": "^13.0.0",
    "del-cli": "^0.2.0",
    "jade": "^1.11.0",
    "jadeify": "^4.6.0",
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "node-sass": "^3.4.2",
    "npm-run-all": "^1.7.0",
    "uglify-js": "^2.6.2",
    "watchify": "^3.7.0"
  }
}
